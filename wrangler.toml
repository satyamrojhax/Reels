name = "reels"
compatibility_date = "2024-01-01"

[build]
command = "npm run build"
cwd = "."
watch_dirs = ["src"]
watch_files = ["package.json", "vite.config.ts"]

[build.environment]
NODE_VERSION = "18"

[env.production]
routes = [
  { pattern = "/*", custom_domain = "reels.pages.dev" }
]
